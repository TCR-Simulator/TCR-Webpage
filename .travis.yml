language: node_js
node_js:
  - "8"

before_deploy:
  - npm run build
  - cd build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  keep-history: true
  on:
    branch: master
